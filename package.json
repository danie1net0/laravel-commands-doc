{
  "name": "laravel-commands-doc",
  "scripts": {
    "dev": "vuepress dev src",
    "build": "NODE_ENV=production NODE_OPTIONS=--openssl-legacy-provider vuepress build src"
  },
  "dependencies": {
    "vuepress": "^1.5.3"
  }
}
